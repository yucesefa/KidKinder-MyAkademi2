
@{
    Layout = null;
}

<!DOCTYPE html>
<html lang="en">

@Html.Action("PartialHead", "Default")

<body>

    @Html.Action("PartialNavbar", "Default")

    <!-- Header Start -->
   @Html.Action("ContactHeaderPartial","Contact")
    <!-- Header End -->
    <!-- Contact Start -->
    <div class="container-fluid pt-5">
        <div class="container">
            <div class="text-center pb-2">
                <p class="section-title px-5">
                    <span class="px-2">İletişime Geç!</span>
                </p>
                <h1 class="mb-4">Herhangi Bir Soru İçin İleitşime Geçin</h1>
            </div>
            @Html.Action("ContactProcessPartial","Contact")
        </div>
    </div>
    <!-- Contact End -->
    <!-- Footer Start -->



    @Html.Action("PartialFooter", "Default")

    @Html.Action("PartialScripts", "Default")

</body>
</html>